import PromoSubmissionForm from '../components/PromoSubmissionForm';

export default function PromoCreateAd() {
  return (
    <PromoSubmissionForm
      fixedType="ad"
      title="Create New Ad"
      subtitle="Create an ad campaign and submit it for approval."
    />
  );
}
